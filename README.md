# serverless_project
Serverless Employee Management System using AWS. Add and view employee details (ID as string, Name, Department, Salary) with DynamoDB, Lambda (GET/POST), API Gateway, and S3-hosted frontend. Includes form validations and a fully serverless architecture.
